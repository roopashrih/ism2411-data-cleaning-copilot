Reflection

Using Copilot, I was able to work on two different functions. This includes handle_missing_values and remove_invalid_rows. The code for these functions were prompted by the comment and function name I put in.

After Copilot generated the code for each of the functions, I modified them with my own knowledge. I renamed the variables for the functions using names that were more understandable and easy to recall in the context. For some of them, I added extra steps to refine the code. These changes were needed to make sure the code came together overall.

I've learned a lot about data cleaning in Python and using Copilot as a tool. For data cleaning, I realized that it's important to remove invalid entires and get rid of random elements to make sure the analysis runs properly. A specific example was converting price and quantity from strings to numeric values. Using Copilot speed up the process and provide a good foundation to continue from. Since it focuses only on a certain section of the code at a time, it's still important to make sure your code flows overall.
